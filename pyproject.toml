[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chromium_osx_tools"
version = "0.1.0"
description = "Tools to x-compile Chromium on Linux"

[tool.black]
exclude = '''
/(
    env
)/
'''

[tool.setuptools.packages.find]
where = ["src"]